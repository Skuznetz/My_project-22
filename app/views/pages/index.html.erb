<h1> Страницы</h1>
<table class="table">
<thead>
<tr>
<th>Название</th>
</tr>
</thead>
<tbody>
  <% @pages.each do |page| %>
  <tr>
   <td>
    <%= page.name %>
   </td>
   <td>
     <%= link_to 'edit',edit_page_path(page) %>
   </td>
   <td>
   <%= linc_to 'destroy',page_path(page), method: :delete, data: {confirm: 'Вы уверены?'} %>
   </td>
   </tr>
   <% end %>
   </tbody>
   </table>